{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run python file",
            "type": "shell",
            "command": "while inotifywait -e close_write ${file}; do ./bin/python3.9 ${file}; done"
        }
    ]
}